#!/bin/bash
#
# configuration options for structr-ui
#

RUN_OPTS="-cp lib/*:${artifactId}-${version}.war"
JAVA_OPTS="-Xms1g -Xmx1g -XX:+UseNUMA"
MAIN_CLASS="org.structr.Ui"
DISPLAY_NAME="${artifactId}-${version}"
LOG_FILE="logs/server.log"
PID_FILE="server.pid"
