# =============================================================================
# GitLab Runner Configuration
# Edit these values before running setup.sh
# =============================================================================

RUNNER_NAME="${RUNNER_NAME:-$(hostname)-runner}"
GITLAB_URL="${GITLAB_URL:-https://gitlab.structr.com}"
RUNNER_BASE_DIR="${RUNNER_BASE_DIR:-$(pwd)}"
RUNNER_IMAGE="${RUNNER_IMAGE:-structr-test-runner:latest}"
CONCURRENT_JOBS="${CONCURRENT_JOBS:-2}"