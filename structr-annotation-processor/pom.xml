<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<name>Structr Annotation Processor</name>
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.structr</groupId>
		<artifactId>structr</artifactId>
		<version>7.0-SNAPSHOT</version>
	</parent>

	<artifactId>structr-annotation-processor</artifactId>
	<packaging>jar</packaging>
	<version>7.0-SNAPSHOT</version>

	<description>
		Compile-time annotation processor that builds a per-module index of @Documentation
		annotated types (META-INF/structr/documented-classes). The index lets Structr discover
		documented classes at runtime without scanning the whole class path, which is required
		for running on the Java module path. This module has no dependencies and must not run
		any annotation processor itself (it is the processor).
	</description>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<!-- this module IS the processor; never run annotation processing on it
					     (also clears the inherited annotationProcessorPaths self-reference) -->
					<proc>none</proc>
					<annotationProcessorPaths combine.self="override"/>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
