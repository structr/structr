After defining a first version of the data model, the next step is usually to build a user interface. This can be done in the `Pages` area.

Next:
- You can create or import pages
- templates, HTML structure
- visibility flags
- admin user?
- ...