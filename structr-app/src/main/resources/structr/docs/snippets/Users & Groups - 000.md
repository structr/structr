### Accessing the Admin User Interface

Only admin users can log into the Admin User Interface. If you try to log in with a non-admin user, you will get the error message `User has no backend access.` and the login will fail.


To be mentioned: the `skipSecurityRelationships` flag.